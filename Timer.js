var Timer = function () {
  
  // ================================================================
  // == PRIVATE =====================================================
  // ================================================================
  
  var running = false;
  var start = null;
  var t = 0;             // time elapsed in ms (cumulative, i.e. counting all starts and stops)
  
  
  // ----------------------------------------------------------------
  function construct() {}
  
  
  // ================================================================
  // == PUBLIC ======================================================
  // ================================================================
  
  var pub = {
    
    // ----------------------------------------------------------------
    reset: function () {
      t = 0;
    },
    
    
    // ----------------------------------------------------------------
    start: function () {
      running = true;
      start = new Date();
    },
    
    
    // ----------------------------------------------------------------
    stop: function () {
      t += (new Date()).getTime() - start.getTime();
      start = null;
      running = false;
    },
    
    
    // ----------------------------------------------------------------
    getMs: function () {
      return (!running ? t : t + (new Date()).getTime() - startDate.getTime());
    }
  };
  
  
  // ================================================================
  // == CONSTRUCT ===================================================
  // ================================================================
  
  construct();
  return pub;
};
